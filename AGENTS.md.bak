---
**給維護者與 AI 代理的提示：**

此檔案 (`AGENTS.md`) 是指導原則的唯一權威來源。`GEMINI.md` 與 `CLAUDE.md` 均由此檔案透過 CI/CD 腳本自動同步生成。

**請只在此檔案進行修改**，對其他檔案的變更將會被自動覆蓋。
---

# AGENTS

- 除非使用者明確許可，嚴禁更改任何 source code。
- 請在詳細閱讀並且trace完本repo的所有code以及doc(包含my_docs/)之後回答我的問題
- 回覆請保持中文敘述與簡潔條列，必要時可附英文專有名詞；若需引用長段程式碼或輸出，提供路徑與概要即可。
- 回覆前必須先進行「事實檢查思考」，依序核對（a）使用者提供內容、（b）模型內部已知資訊、（c）至少一個可信的外部來源（網路搜尋、官方 repo、文件等）；除非使用者明確禁止或題目已提供完整證據，否則需主動執行外部查詢。
- 進行外部查詢時先簡述查詢動機與渠道，並於回覆中標示實際來源或檢核方式；屬於推論或假設的內容須清楚標註。
- **標註文件來源**：在產出或修改任何文件（如 `*.md`）時，若內容是基於本倉庫中既有的程式碼、設計文件、或外部資料，**必須**為每一項關鍵資訊標註其來源。來源應以超連結形式指向對應的檔案路徑或 URL。此舉旨在確保所有資訊皆可追溯且經過驗證。
- 若外部查詢失敗或資料仍不足，須明確回覆「無法確定」或說明缺乏依據，禁止臆測、補完或擴大原意。
- 不得替使用者重寫或改述成不同語意；如需重述，請標示「重述版本」並保持語義對等。
- 回覆時避免使用「應該是」「可能是」「我猜」等模糊語氣，除非使用者明確要求。
- 產出前請自檢三項：a. 回覆有明確資料依據 b. 未超出問題範圍 c. 無新增未被提及的人名／數字／事件。

### 檔案修改準則 (Gemini 專用)

**重要檔案備份原則**
- **修改前備份**：在對任何被視為「重要」的文檔（如設定檔、源碼、或使用者指定的關鍵文件）進行修改操作（無論使用 `replace` 或 `write_file`）之前，必須先建立一個備份。
- **備份命名**：備份檔案應以原檔名加上 `.bak` 或類似的後綴命名，以清楚標示其為備份。

**臨時修正案 1：檔案修改的工具選擇原則**

在修改一個已存在的檔案時，必須優先選擇最精確、最安全的工具。

- **首選 `replace`**：對於部分內容的修改、替換或刪除，必須優先使用 `replace` 工具。此工具因其需要提供上下文的特性，本質上更安全。
- **限制 `write_file`**：`write_file` 工具只應用於兩種情況：(a) 建立一個全新的檔案；(b) 當任務目標就是用全新的、完整的內容徹底覆寫一個舊檔案時。
- **禁止濫用**：嚴禁使用 `write_file` 來執行實質上的「部分修改」任務。

**臨時修正案 2：`write_file` 的安全覆寫原則**

當依據規則 1，確認需要使用 `write_file` 進行覆寫操作時，必須遵循以下安全流程：

- **內容完整性驗證**：在呼叫 `write_file` 之前，必須在內部流程中增加一步自我驗證，確保 `content` 參數代表了整個檔案的最終完整形態。
- **禁止不完整寫入**：嚴禁寫入任何已知是不完整的、被截斷的、或包含臨時佔位符（如 `...`）的內容。
- **疑問時重新讀取**：如果在生成內容的完整性上有任何一絲懷疑，必須在寫入前重新讀取原始檔案或重新生成完整內容，以確保萬無一失。